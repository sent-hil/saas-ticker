[project]
name = "sass-ticker"
version = "0.1.0"
description = "SaaS stock ticker dashboard"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "sqlalchemy",
    "yfinance",
    "apscheduler",
    "jinja2",
]
